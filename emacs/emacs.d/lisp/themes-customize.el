;;; Summary: These are just some of the 9 billion themes I love to have
;;; installed <3.

;; Material theme.
(use-package material-theme :ensure t :defer nil)

;; Solarized theme.
(use-package color-theme-solarized :ensure t :defer t)

;; Idea Darcula theme.
(use-package idea-darkula-theme :ensure t :defer t)

;; Tangotango theme.
(use-package tangotango-theme :ensure t :defer t)

;; Gotham (!) theme.
(use-package gotham-theme :ensure t :defer t)

;; Atom One Dark theme.
(use-package atom-one-dark-theme :ensure t :defer t)

;; Foggy Night theme.
(use-package foggy-night-theme :ensure t :defer t)

;; Gruvbox theme.
(use-package gruvbox-theme :ensure t :defer t)

;; Inkpot theme.
(use-package inkpot-theme :ensure t :defer t)

;; Jellybeans (!) theme.
(use-package jbeans-theme :ensure t :defer t)

;; Zenburn theme.
(use-package zenburn-theme :ensure t :defer t)

;; Monokai theme.
(use-package monokai-theme :ensure t :defer t)

;; Molokai theme.
;;
;; Yes, this is different than monokai.  Generally, this has ligher contrast
;; colors in comparison and tends to be very popular in OSX terminals.
(use-package molokai-theme :ensure t :defer t)

;; Mustang theme.
;;
;; A nicer version of Late Night, imo, using white, grey, and orange.
(use-package mustang-theme :ensure t :defer t)

;; Obsidian theme.
;;
;; This is like a darker material.
(use-package obsidian-theme :ensure t :defer t)

;; Planet (!) theme.
;;
;; A softer, bluer solarized.
(use-package planet-theme :ensure t :defer t)

;; Railcasts theme.
;;
;; Because you're not cool if you don't have this one.
(use-package railscasts-theme :ensure t :defer t)

;; Spacegray theme.
(use-package spacegray-theme :ensure t :defer t)

;; Warm Night theme.
(use-package warm-night-theme :ensure t :defer t)

(provide 'themes-extra)
